dependencies {
    compileOnly 'org.springframework.boot:spring-boot-starter'
}
def deps = [
        "service@io.zephyr.tutorials:spring-base:1.0.0-SNAPSHOT",
        "service@io.zephyr.tutorials:test-plugin-1:1.0.0-SNAPSHOT",
].join(',')
war {
    manifest {
        attributes(
                'group': 'io.zephyr.tutorials',
                'name': 'test-plugin-2',
                'version': '1.0.0-SNAPSHOT',
                'dependencies': deps,
                'description': 'just a test plugin!',
                'type': 'plugin'
        )
    }
}
